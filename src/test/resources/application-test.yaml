# Prefer console logging for tests over writing to a file.
logging:
  console:
    enabled: true
  file:
    name:

spring:
  ai:
    mcp:
      server:
        version: test-SNAPSHOT
  http:
    serviceclient:
      accuweather:
        default-header:
          Authorization: Bearer MY_TEST_API_KEY